# AGENTS.md

## 1. このプロジェクトで実施すること
- 研修課題として、C# WinForms の販売管理アプリを実装する。
- アプリは以下を一元管理できることを目的とする。
  - 商品マスタ
  - 在庫
  - 売上登録
  - 売上集計
  - CSV の読込/出力
- 想定ユーザーは「本部店舗管理部（PCで簡単な業務アプリを操作できるレベル）」。

## 2. スコープ
### In Scope（初版で必須）
- 商品管理
  - 商品ID一意
  - 商品名/単価/区分の保持
  - 登録/編集/削除
  - 必須入力チェック
  - 単価は 0 以上
  - 一覧表示
- 在庫管理
  - 入荷/出庫
  - 在庫残数確認
- 売上管理
  - 売上登録（販売情報の記録）
- 売上集計
  - 期間別集計
  - 商品別集計
- データ管理
  - CSVファイルの読込/出力

### Out of Scope（初版では任意）
- SQLite の必須採用
- 追加課題（フィルタ機能、商品名重複禁止、在庫履歴、在庫算出方式変更）の必須化

## 3. 技術方針
- UI/アプリ形態は C# WinForms のデスクトップアプリとする。
- 永続化は CSV 優先とする。
- SQLite は拡張対応として任意に採用可（初版では必須にしない）。
- 学習要素（async/await、lock、LINQ、Dictionary/List、Action/Func、型変換、拡張メソッド）は、実装で自然に使える範囲で採用する。

## 4. 要件整理と仕様決定ルール
- 仕様の判断に迷う場合は、デファクトスタンダード（Windows/Excel等の一般的な挙動）を優先する。
- 要求者判断が必要な仕様変更は、指導担当を要求者代理として決定する。
- 「講師陣向け（受講生に配布時に削除）」シートは参考情報であり、受講生向け拘束要件にはしない。

## 5. 開発フロー（厳密運用）
1. 要求/要件の整理
2. 設計（画面、データ、主要処理、エラー方針）
3. タスク分解（依存関係、優先順位、見積り）
4. 実装（チケット単位）
5. テスト観点の確認と検証

- 不確実性が高いタスクを優先して着手する。
- タスクはできるだけ小さく分割し、レビュー可能な単位で進める。

## 6. Git/ブランチ運用
- `main` への直接コミットは禁止。
- 統合先は `develop`。
- 作業ブランチは `feature/<topic>` を使用する。
- 原則として 1 タスク 1 PR とする。

## 7. PR作成ルール
- PRの言語は日本語。
- タイトル形式は `type: 概要`（例: `docs: AGENTS.md 初版追加`）。
- PR本文は以下3項目を必須とする。
  - 目的
  - 変更点
  - 確認結果
- マージ条件
  - レビュー承認済み
  - CI成功
  - squash merge

## 8. 品質基準
- 変更内容がどの要件に対応するかをPRで明示する。
- 入力エラー/業務エラー時の挙動を実装時に定義し、検証結果を残す。
- データ保護と継続利用を意識し、破壊的変更時は移行手順を明示する。
- 将来的な機能追加を想定し、モジュール責務を分離した設計を優先する。

## 9. 開発環境・初期プロジェクト構成
- 開発環境
  - Visual Studio（.NET デスクトップ開発）
  - .NET Framework 4.7.2
- ソリューション/プロジェクト配置
  - `Sales Management App/Sales Management App.slnx`
  - `Sales Management App/Sales Management App/Sales Management App.csproj`
- エントリポイント
  - クラスファイル: `Sales Management App/Sales Management App/Program.cs`
- 運用ルール
  - `bin/`, `obj/`, `.vs/` などのビルド生成物はコミットしない。

## 10. テスト方針（TDD）
- 本プロジェクトは TDD（Red-Green-Refactor）で実装を進める。
- 実装着手前に、要件に対応する失敗テストを先に作成する。
- 失敗テストを最小実装で成功させた後、リファクタリングを実施する。
- 不具合修正時も再発防止テストを先に追加してから修正する。
- テストフレームワークは NUnit を使用する。
- テストコードは `tests/` 配下に配置する。

## 11. PR品質ゲート（GitHub Actions）
- Pull Request では GitHub Actions による以下の自動チェックを必須とする。
  - WinForms アプリの build
  - NUnit テストの実行
- 品質ゲートが失敗している PR はマージしない。

## 12. ルート配置データの運用
- 初期データ/運用データはリポジトリ直下に配置する。
  - `products.csv`
  - `inventory.csv`
  - `sales_yyyyMMdd.csv`
  - `週次売上集計ファイル.xlsx`
- WinForms 起動時は `AppDomain.CurrentDomain.BaseDirectory` から親ディレクトリを探索し、`AGENTS.md` があるディレクトリをリポジトリルートとして判定する。
- 売上CSVは次の2形式を受け入れる。
  - `SaleDate,StoreId,ProductId,Quantity`
  - `SaleDate,StoreId,ProductId,Quantity,SalesAmount`
- 4列売上CSVを読み込んだ場合は、`products.csv` の単価を使って `SalesAmount` を算出し、5列形式へ正規化して保存する。
- 売上CSVは `sales_yyyyMMdd.csv` 形式のみ読込対象とし、最新日付ファイルを優先する。

## 13. 設計/実装ルール（SRP・DRY）
- クラス/メソッドは単一責務を維持し、責務が増えた場合は分割する。
- 重複ロジックは共通化し、同一処理を複数箇所へコピペしない。
- クラス内クラス（nested class）は禁止し、必要な型は独立 `.cs` に切り出す。
- WinForms の肥大化を避けるため、画面コードは `partial class` やサービスへ責務分離する。
- 既存仕様を変えない構造リファクタを優先し、仕様変更はIssue単位で明示して実施する。
